name: food_orders_store_presentation_screen
description: "An application that simulate clients ordering dishes. The disches are then put into ovens for a certain duration awaiting to be retrieved by clients."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
# Provider State Manager dependency
  provider : ^6.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
